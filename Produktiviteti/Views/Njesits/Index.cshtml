@model IEnumerable<Produktiviteti.Models.Njesit>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Njesit_Etc)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Njesit_Etc)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NjesitId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NjesitId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NjesitId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
