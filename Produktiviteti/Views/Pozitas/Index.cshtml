@model IEnumerable<Produktiviteti.Models.Pozita>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Pozita_punes)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Pozita_punes)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PozitaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PozitaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PozitaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
